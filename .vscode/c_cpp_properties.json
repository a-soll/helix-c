{
    "configurations": [
        {
            "name": "Mac",
            "includePath": [
                "${default}",
                "${workspaceRoot}/lib",
                "${workspaceRoot}/include"
            ],
            "browse": {
                "path": [
                    "${default}",
                    "${workspaceRoot}/include",
                    "${workspaceRoot}/lib"
                ]
            }
        }
    ],
    "version": 4
}
